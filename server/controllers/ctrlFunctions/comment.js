const { generateAccessToken, sendAccessToken, isAuthorized } = require('../tokenFunctions');

module.exports = {
  writeComment: (res, req) => {

  },

  seeComment: (res, req) => {
    
  },

  addReaction: (res, req) => {
    
  }
}