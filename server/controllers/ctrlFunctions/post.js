const { generateAccessToken, sendAccessToken, isAuthorized } = require('../tokenFunctions');

module.exports = {
  writePost: (res, req) => {

  },

  seePost: (res, req) => {
    
  },

  deletePost: (res, req) => {
    
  },

  updatePost: (res, req) => {
    
  },

  updateReaction: (res, req) => {
    
  },
}